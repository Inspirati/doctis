# Doctis Docker Demo

This directory provides a simple Docker-based demo environment for running Doctis without needing a VM or dedicated Linux installation.

## Requirements
- Docker
- Docker Compose

## Start demo

sudo apt install docker.io

cd docker (this directory)
docker compose up --build
